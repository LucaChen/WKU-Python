# 实验说明 · 模型私有化部署与 Serving

你将创造什么：完成本课实验后，应能做到——探活通过，模型列表含 qwen2.5，JSON 约束输出 ping=ok。

课堂讲义：打开 `chapter13_模型私有化部署与Serving_学生讲义.ipynb`。每个知识点有理论、案例和练习；综合练习 P1 使用 `agent_lab`。

课后练习：打开 `chapter13_模型私有化部署与Serving_课后练习.ipynb`。P1、P2 必做，P3 选做。不要依赖讲义里已经构造好的变量。

可选阅读同目录 serving.env.example。不要把真实密钥写进笔记本。

验收脚本：在本文件夹执行 `python3 experiment.py`，看到 `本课验收通过`。

阶段与评分：阶段零 · 前置基座（前置基座，为后续任务提供稳定 /v1 端点）。时间盒见本课 `教案.md`。
