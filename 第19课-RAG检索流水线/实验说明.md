# 实验说明 · RAG 检索流水线

你将创造什么：完成本课实验后，应能做到——知识库完成分块入库；Retriever 封装为 Tool 并可被调用；续航类问题命中手册切片。

课堂讲义：打开 `chapter19_RAG检索流水线_学生讲义.ipynb`。每个知识点有理论、案例和练习；综合练习 P1 使用 `agent_lab`。

课后练习：打开 `chapter19_RAG检索流水线_课后练习.ipynb`。P1、P2 必做，P3 选做。不要依赖讲义里已经构造好的变量。

课后请优先读取本课 kb/ 目录，不要依赖讲义里已经建好的 retriever 变量。

验收脚本：在本文件夹执行 `python3 experiment.py`，看到 `本课验收通过`。

阶段与评分：阶段四 · 任务三 3.1 / 15分（15分）。时间盒见本课 `教案.md`。
